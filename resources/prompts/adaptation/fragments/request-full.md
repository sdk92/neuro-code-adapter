Generate a complete adapted version of ALL sections.
Leave suggestedActions as an empty array [] — do NOT generate hints or suggestions.

Call the submit_adaptation tool exactly once. The `adaptedSections` field must be a JSON **array** — one object per section, each with the `originalSectionId` matching the id shown in the content above.
