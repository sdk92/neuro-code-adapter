Generate a complete adapted version of ALL sections according to the task granularity.
Leave suggestedActions as an empty array [] — do NOT generate hints or suggestions.

Respond with ONLY valid JSON matching the AdaptationResponse schema.
