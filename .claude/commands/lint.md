---
description: Run ESLint over the src/ tree
allowed-tools: Bash, Read
---

Run `npx eslint src --ext ts` and report results.

Group findings by file. For each finding, show: line:col, rule name, message. Do not fix anything unless the user asks.
